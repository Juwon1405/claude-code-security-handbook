# 이 케이스의 규칙

- 증거 파일은 읽기만 한다. 어떤 경우에도 `evidence/` 아래를 고치지 않는다.
- 판단마다 근거가 된 로그 라인을 원문 그대로 한 줄 인용한다.
- 로그만으로 확정할 수 없는 것은 "미확정" 으로 따로 적는다. 추측을 사실로 쓰지 않는다.
- 시각은 UTC 로 통일해 적는다. `access.log` 는 +0900 이므로 변환해서 쓴다.
